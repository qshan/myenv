#!/usr/bin/env bash

sudo apt-get install fcitx-googlepinyin

#Configure language support
# select the language support and select items
#   select chinese language
#   select fcitx as the Keyboard input methods system

# reboot now
