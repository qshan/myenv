# 1.2 核心配置与文档模板
# 模板 1：SKILL.md（遵循 Agent Skills 开放标准）
# 采用「YAML 元数据头 + Markdown 执行指令」的格式，既可以被 Agent 自动解析加载，也适合人工阅读维护。
# Reference web: https://github.com/strativd/ai-skills
# Details: keep --- in first line
==============================



---
name: query-user-info
description: 根据用户ID查询用户的姓名、部门、职位等基础信息，仅支持通过合法用户ID查询，禁止模糊匹配。
version: 1.0.0
author: 技术中台
license: Apache-2.0
user-invocable: true
compatibility: 依赖内部用户中心 API
tags: ["用户管理", "基础查询", "只读操作"]
---

# 用户信息查询 Skill

## 核心执行规则
1. 必须传入合法的用户ID（格式为 u+数字，如 u001），否则直接返回参数错误；
2. 仅返回姓名、部门、职位三个字段，禁止返回手机号、邮箱等敏感信息；
3. 单次仅支持查询一个用户，批量查询需调用批量用户查询 Skill。

## 执行流程
1. 校验 user_id 格式是否符合规范；
2. 调用用户中心 API 查询数据；
3. 脱敏处理后返回标准化结果。

## 入参说明
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| user_id | string | 是 | 用户唯一ID，格式 u+数字 |

## 出参说明
| 字段名 | 类型 | 说明 |
|--------|------|------|
| name | string | 用户姓名 |
| dept | string | 所属部门 |
| position | string | 职位 |

## 异常处理
- 用户不存在：返回「用户ID不存在，请确认后重试」；
- 接口超时：自动重试 2 次，仍失败则返回「查询服务暂时不可用」。

## 使用示例
输入：{"user_id": "u001"}
输出：{"name": "张三", "dept": "技术部", "position": "高级工程师"}
